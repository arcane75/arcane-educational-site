import React from 'react';
import MenuBar from '../MenuBar/MenuBar';

const Instructor = () => {
    return (
        <div>
            <MenuBar></MenuBar>
        </div>
    );
};

export default Instructor;