
import Footer from '../Footer/Footer';
import MenuBar from '../MenuBar/MenuBar';

const Instructor = () => {

    return (
        <div>
            <MenuBar></MenuBar>
            <Footer></Footer>
        </div>
    );
};

export default Instructor;