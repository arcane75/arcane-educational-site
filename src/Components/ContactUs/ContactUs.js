import React from 'react';
import MenuBar from '../MenuBar/MenuBar';

const ContactUs = () => {
    return (
        <div>
            <MenuBar></MenuBar>
        </div>
    );
};

export default ContactUs;