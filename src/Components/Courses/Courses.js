import React from 'react';
import MenuBar from '../MenuBar/MenuBar';

const Courses = () => {
    return (
        <div>
            <MenuBar></MenuBar>
        </div>
    );
};

export default Courses;